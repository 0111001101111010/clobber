'use strict';

var clobber = require('../lib/clobber.js');
var assert = require('should');

describe('clobber', function () {

  it('should be awesome', function () {
    clobber.awesome().should.equal('awesome');
  });

});
